package test;
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.io.IOException;
import java.util.Arrays;
import java.util.StringTokenizer;
import java.util.Stack;
import java.util.Vector;
import java.util.Collections;
import java.util.Comparator;
public class Main{
	
	
    public static void main(String[] args)throws IOException{
    	class tuple implements Comparable<tuple>{
    		public int x;
    		public int y;
    		tuple(int a , int b ){
    			x=a;
    			y=b;
    		}
    		@Override
    		public int compareTo(tuple t) {
    			return this.y - t.y;
    		}
    	};
    	class MemberComparator implements Comparator{
    		  public int compare(Object arg0, Object arg1) {
    		   return ((tuple)arg0).x - ((tuple)arg1).x;
    		 }
    		}
    
		BufferedReader testReader = new BufferedReader(new InputStreamReader(System.in));

		Vector<tuple> t_vector = new Vector();
		Vector<tuple> test_vector = new Vector();
		
		int case_num = Integer.parseInt(testReader.readLine());
		StringTokenizer st;
		for(int i = 0; i < case_num; i++ ) {
			st = new StringTokenizer(testReader.readLine());
			int start = Integer.parseInt(st.nextToken());
			int end = Integer.parseInt(st.nextToken());
			t_vector.add(new tuple(start,end));
			
		}
		Collections.sort(t_vector);
		int end_befor =0;

		int num_of_can = 0;
		for(int i = 0; i < case_num; i++) {
			if(i<case_num-1) {
				int start_now = t_vector.get(i).x;
				int end_now = t_vector.get(i).y;
				int start_next = t_vector.get(i+1).x;
				int end_next = t_vector.get(i+1).y;
				if(end_befor <= start_now) {
					if(start_now == start_next) {
						if(end_now==end_next) {
							num_of_can++;
						}
					}
				}
				
			}
		}
		try {
			BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
			for(int i = 0; i < test_vector.size(); i++) {
				bw.write(test_vector.get(i).x+"");
				bw.write(" ");
				bw.write(test_vector.get(i).y+"");
				bw.write("\n");
			}
		


			bw.close();
		} catch(IOException e){
			e.printStackTrace();
		}
		
    }
}
